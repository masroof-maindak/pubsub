[project]
name = "pubsub"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "questionary>=2.1.1",
    "websockets>=16.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.4",
]
